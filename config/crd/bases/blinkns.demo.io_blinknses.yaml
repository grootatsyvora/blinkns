---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: blinknses.blinkns.demo.io
spec:
  group: blinkns.demo.io
  names:
    kind: BlinkNS
    listKind: BlinkNSList
    plural: blinknses
    shortNames:
    - bns
    singular: blinkns
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ttl
      name: TTL
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.expiresAt
      name: Expires
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BlinkNS provisions a Kubernetes namespace that is automatically
          deleted when its TTL expires.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BlinkNSSpec defines the desired state of a BlinkNS resource.
            properties:
              labels:
                additionalProperties:
                  type: string
                description: Labels are applied to the created namespace.
                type: object
              notifications:
                description: Notifications configures a warning alert sent at ttl/10
                  before expiry.
                properties:
                  webhookSecretRef:
                    description: |-
                      WebhookSecretRef is the name of a Secret in the blinkns-system namespace.
                      The Secret must have a key named "url" containing the webhook URL.
                    type: string
                  webhookType:
                    description: WebhookType is "slack" or "discord".
                    enum:
                    - slack
                    - discord
                    type: string
                required:
                - webhookSecretRef
                - webhookType
                type: object
              ttl:
                description: |-
                  TTL is how long the namespace lives before being deleted.
                  Accepts any positive integer with a unit: m, h, d, w, mo, y.
                  Examples: "30m", "12h", "189h", "20d", "1w", "6mo", "1y".
                  Minimum: 1m. Maximum: 1y (8760h). Immutable after creation.
                type: string
            required:
            - ttl
            type: object
          status:
            description: BlinkNSStatus defines the observed state of a BlinkNS resource.
            properties:
              conditions:
                description: Conditions are the latest observations of the BlinkNS
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              createdAt:
                description: CreatedAt is when the namespace was provisioned.
                format: date-time
                type: string
              expiresAt:
                description: ExpiresAt is when the namespace will be deleted.
                format: date-time
                type: string
              notificationSent:
                description: NotificationSent is true once the warning notification
                  has been delivered.
                type: boolean
              phase:
                description: Phase is the current lifecycle state.
                type: string
              warningAt:
                description: WarningAt is when the pre-expiry notification fires (expiresAt
                  - ttl/10).
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
